/**
 * Compatibility facade for the former Control-owned Routing tab strip.
 * Production Routing navigation is `RoutingTabStrip` in `routing-tab-strip.tsx`.
 */
export { RoutingTabStrip as ControlTabStrip } from "./routing-tab-strip";
